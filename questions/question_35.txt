問題35: 関数を値として渡す

`int` 型の引数を2つ取り `int` を返す関数と、`int` 型の引数を2つ（`a`, `b`）取ります。
渡された関数を使って `a` と `b` を計算し、その結果を返す `calc` という関数を定義してください。

`add(a, b int) int` と `subtract(a, b int) int` という2つの関数を定義し、これらを `calc` 関数に渡して `10` と `5` の足し算と引き算の結果をそれぞれコンソールに表示するプログラムを作成してください。
