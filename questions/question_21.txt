問題21: `if` と `else`

`int` 型の変数 `score` に `85` を代入します。
`if-else if-else` 構文を使って、`score` の値に応じて以下の条件で成績を判定し、結果をコンソールに表示するプログラムを作成してください。

- `score` が 80 以上なら "Great"
- `score` が 60 以上なら "Good"
- それ以外なら "OK"
