問題69: `regexp` によるマッチ

`regexp` パッケージを使って、文字列が特定の正規表現パターンにマッチするかどうかを判定するプログラムを作成してください。

正規表現パターンとして `p([a-z]+)ch` を使います。（例: "peach", "pitch", "punch" にマッチ）
`regexp.MatchString` を使って、文字列 "peach" がこのパターンにマッチするかどうかを確認し、結果（`true` または `false`）とエラーをコンソールに表示してください。
