問題42: 型にメソッドを追加

`type MyInt int` のように、`int` 型を基にした新しい型 `MyInt` を定義してください。
この `MyInt` 型に対して、その値が2の倍数であるかどうかを判定する `isEven()` というメソッドを追加してください。

`main` 関数で `MyInt` 型の変数に値を設定し、`isEven()` メソッドを呼び出して結果（`true` または `false`）をコンソールに表示するプログラムを作成してください。
