問題24: `switch` (式なし)

`int` 型の変数 `hour` に `14` を代入します。
式のない `switch` 文（`switch true` と等価）を使って、`hour` の値に応じて以下の時間帯を判定し、結果をコンソールに表示するプログラムを作成してください。

- `hour` が 12 未満なら "Morning"
- `hour` が 17 未満なら "Afternoon"
- それ以外なら "Evening"
