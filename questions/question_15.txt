問題15: Goの構造体をJSONに変換 (`json.Marshal`)

`ID` (int) と `Name` (string) フィールドを持つ `Product` 構造体を定義します。
この構造体のインスタンスを作成し、`encoding/json.Marshal` を使ってJSON形式のバイトスライスに変換し、それを文字列としてコンソールに表示するプログラムを作成してください。

JSONのキーが小文字になるように、構造体タグ (`json:"..."`) も設定してください。
