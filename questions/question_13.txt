問題13: マップの基本 (作成、追加、アクセス)

キーが `string` 型、値が `int` 型のマップを作成し、以下の操作を行うプログラムを作成してください。

1. `make` を使ってマップを初期化します。
2. マップにキー "apple"、値 `100` を追加します。
3. マップにキー "banana"、値 `200` を追加します。
4. キー "apple" の値を取得して表示します。
5. `for-range` ループを使って、マップのすべてのキーと値を表示します。
