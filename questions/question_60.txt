問題60: `time.Timer`

`time.NewTimer` を使って、2秒後に一度だけイベントを発生させるタイマーを作成します。
`select` 文を使って、タイマーのチャネルからの受信を待機し、受信したら "Timer expired!" とコンソールに表示するプログラムを作成してください。

タイマーが切れる前に、ユーザーが何かキーを入力したらタイマーを停止 (`Stop()`) させて "Timer stopped" と表示する、といった応用も考えてみましょう。（この問題では、単純に2秒待つだけで構いません）
