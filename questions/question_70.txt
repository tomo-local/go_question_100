問題70: `regexp` による置換

`regexp` パッケージを使って、文字列内の一部を別の文字列に置換するプログラムを作成してください。

文字列 "a peach" と "a banana" があるとします。
正規表現 `a `（"a" とスペース）にマッチする部分を、"an " に置換してください。

`regexp.Compile` で正規表現オブジェクトを作成し、そのオブジェクトの `ReplaceAllString` メソッドを使って置換を行い、結果をコンソールに表示してください。
