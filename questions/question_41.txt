問題41: 構造体のメソッド

`width` と `height` という `int` 型のフィールドを持つ `Rectangle` 構造体を定義してください。
この構造体に、その面積を計算して返す `Area()` という名前のメソッドを追加してください。

`main` 関数で `Rectangle` のインスタンスを作成し、`Area()` メソッドを呼び出して計算結果をコンソールに表示するプログラムを作成してください。
