問題4: スライスと `append`

`append` 関数を使って、スライスを操作するプログラムを作成してください。

1. `[]int{1, 2, 3}` という初期スライスを作成します。
2. `append` を使って、末尾に整数 `4` を追加します。
3. `append` を使って、別のスライス `[]int{5, 6}` のすべての要素を末尾に追加します。
4. 各ステップでのスライスの内容と、可能であればその時点でのスライスの長さ (`len`) と容量 (`cap`) をコンソールに表示してください。