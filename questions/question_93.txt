問題93: アサーションライブラリを使わないテスト

Goの標準の `testing` パッケージのみを使い、外部のアサーションライブラリ（例: `testify/assert`）に頼らずにテストを記述するスタイルを確認します。

これは特定の新しいコードを書く問題ではありません。
問題86から88で見てきたように、Goの標準的なテストは `if got != want` のような単純な比較と、`t.Errorf` や `t.Fatalf` を使ったエラー報告で構成されることが一般的です。

このスタイルがなぜGoの文化で好まれるのか（例: 依存が少ない、明示的である、IDEの機能と連携しやすいなど）、その利点と欠点について考察してみてください。
（解答ファイルに、この考察の例を記述します）
