問題37: ポインタの基本

`int` 型の変数 `x` に `10` を代入します。
`&` 演算子を使って変数 `x` のメモリアドレスを取得し、ポインタ変数 `p` に格納してください。
その後、`*` 演算子を使ってポインタ `p` が指す先の値を読み取り、コンソールに表示するプログラムを作成してください。
また、ポインタ `p` を使って `x` の値を `20` に変更し、再度 `x` の値を表示して、値が変わったことを確認してください。
