問題52: `sync.WaitGroup`

`sync.WaitGroup` を使って、3つのワーパーゴルーチンがすべて処理を完了するまで `main` 関数を待機させるプログラムを作成してください。

各ワーカーゴルーチンは、起動後に1秒間スリープし、"Worker X finished."（Xはワーカー番号）と表示して終了するようにします。
すべてのワーカーが終了した後、`main` 関数は "All workers finished." と表示して終了してください。
