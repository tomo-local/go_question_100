問題84: `exec` による外部コマンドの実行

`os/exec` パッケージを使って、外部コマンド `ls -a -l -h` を実行し、その標準出力を取得してコンソールに表示するプログラムを作成してください。

`exec.Command` でコマンドオブジェクトを作成し、`Output()` メソッドを使ってコマンドを実行し、出力を一度に取得する方法で実装してください。
エラーハンドリングも適切に行ってください。

(Windows環境の場合は `ls` の代わりに `dir` を使うなど、適宜読み替えてください)
