問題75: `log` パッケージ

標準の `log` パッケージを使って、コンソールにログメッセージを出力するプログラムを作成してください。

`log.Println` を使って、情報メッセージ "This is an info message." を出力します。
`log.Fatalf` を使って、致命的なエラーメッセージ "This is a fatal error." を出力します。

`log.Fatalf` がメッセージを出力した後に、プログラムがステータスコード `1` で終了することを確認してください。
