問題50: `io.Reader` インターフェース

`strings.NewReader` を使って、文字列から `io.Reader` インターフェースを満たすインスタンスを作成します。
`io.ReadAll`（Go 1.16以降では `io.ReadAll`、それ以前は `ioutil.ReadAll`）を使って、そのリーダーからすべてのデータをバイトスライスとして読み込み、文字列に変換してコンソールに表示するプログラムを作成してください。

`io.Reader` が、データのソース（この場合は文字列）を抽象化するための強力なインターフェースであることを確認してください。
