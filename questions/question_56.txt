問題56: `range` と `close`

`for-range` ループが、クローズされたチャネルからどのように値を受信するかを確認するプログラムを作成します。

1. `int` 型のチャネルを作成します。
2. 新しいゴルーチンで、そのチャネルに `1`, `2`, `3` という3つの値を送信し、その後チャネルを `close` します。
3. `main` 関数で `for-range` を使ってチャネルから値を受信し、コンソールに表示します。

チャネルがクローズされると `for-range` ループが正常に終了することを確認してください。
