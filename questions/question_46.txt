問題46: 型アサーション

`interface{}` 型の変数に `string` 型の値 "hello" を代入します。
その後、型アサーションを使って、この変数が `string` 型であるかを確認し、そうであればその値を大文字に変換して表示するプログラムを作成してください。

型アサーションは `value, ok := i.(T)` の形式を使い、`ok` が `true` の場合にのみ処理を行うようにしてください。
