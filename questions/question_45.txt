問題45: 空のインターフェース (`interface{}`)

空のインターフェース `interface{}` が、任意の型の値を保持できることを示すプログラムを作成してください。

`describe` という、`interface{}` 型の引数を一つ取る関数を定義します。この関数は、`fmt.Printf` の `%T`（型）と `%v`（値）フォーマット指定子を使って、渡された値の型と値を表示するようにします。

`main` 関数から、`describe` 関数に整数、文字列、`bool` 値をそれぞれ渡して、結果を確認してください。
