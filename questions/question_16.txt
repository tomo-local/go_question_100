問題16: JSONをGoの構造体に変換 (`json.Unmarshal`)

問題15で使う `Product` 構造体をここでも使います。
`{"id":101,"name":"Book"}` というJSON文字列（バイトスライス）を、`encoding/json.Unmarshal` を使って `Product` 構造体のインスタンスに変換（デコード）し、そのインスタンスの各フィールドの値をコンソールに表示するプログラムを作成してください。
