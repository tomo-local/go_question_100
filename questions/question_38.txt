問題38: 関数にポインタを渡す

`int` 型の引数を1つ取る `double` という関数を定義します。ただし、この関数は引数の値を直接2倍にするように、引数として `*int`（`int`へのポインタ）を取るようにしてください。

`main` 関数で `int` 型の変数 `n` に `5` を代入し、`double` 関数に `n` のアドレスを渡してください。
`double` 関数呼び出しの後、`n` の値が `10` になっていることを確認してコンソールに表示するプログラムを作成してください。
