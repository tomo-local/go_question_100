問題92: `Example` 関数

godocなどで表示される、関数の使用例を示すための `Example` 関数を記述する方法を学びます。

`Example` というプレフィックスを持つ関数を定義します。
`fmt.Println` などで何かを標準出力に書き出すコードを記述し、その直後に `// Output:` というコメント行と、期待される出力を記述します。

`add` 関数を例にとり、`ExampleAdd` という`Example`関数を記述してください。
`go test` を実行すると、この出力が正しいかどうかも検証されます。
