問題1: `fmt.Println` vs `fmt.Printf`

`fmt.Println` と `fmt.Printf` の違いがわかるように、以下の2つの処理を行うプログラムを作成してください。

1. `fmt.Println` を使って、文字列 "Hello" と整数 `123` を一度に出力する。
2. `fmt.Printf` を使って、"Name: [名前], Age: [年齢]" というフォーマットで、名前 (文字列) と年齢 (整数) を出力する。名前と年齢には好きな値を指定してください。