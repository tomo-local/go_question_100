問題12: ファイルの読み書き (`os.ReadFile`, `os.WriteFile`)

`os.WriteFile` を使って "hello.txt" というファイルに "Hello, Go!" という文字列を書き込み、その後 `os.ReadFile` を使ってそのファイルの内容を読み込み、コンソールに表示するプログラムを作成してください。

パーミッションは `0644` を指定してください。
